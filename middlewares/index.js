const validateBody = require('./validateBody');
const isValidId = require('./isValidById');
const authentificate = require('./authentificate');

module.exports = { validateBody, isValidId, authentificate };
